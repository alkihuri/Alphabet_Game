using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FinishSceneSettings : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameStates.level = 4;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
